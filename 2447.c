#include <stdio.h>
char star[3][4] = {"***","* *","***"};

void print_star(int n)
{
    for (int i = 1; i <= n; i++)
    {
        if (i == )
    }
}

int main(void)
{
    int N;

    scanf("%d", &N);
    print_star(N);
}